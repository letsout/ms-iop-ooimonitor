<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.asiainfo.msooimonitor.mapper.dbt.common.CommonMapper">
    <select id="getValue" parameterType="string" resultType="string">
        ${sql}
    </select>

    <insert id="insertSql" parameterType="string">
        ${sql}
    </insert>

    <select id="getMap" parameterType="string" resultType="map">
        ${sql}
    </select>

    <delete id="deleteSql" parameterType="string">
        ${sql}
    </delete>
</mapper>